shifts:
  - name: daily-checkin
    cron: "0 9 * * *"
    # No prompt — uses the default clock-in protocol

  # Eval shifts — uncomment to enable
  # - name: eval-example
  #   type: eval
  #   scenario: eval/scenarios/example-scenario.md
  #   enabled: false
